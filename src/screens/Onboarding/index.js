import { View, Text } from 'react-native'
import React from 'react'
import OnboardingComp from '../../components/OnboardingComp'

const Onboarding = ({navigation}) => {
  return (
   <OnboardingComp navigation={navigation} />
  )
}

export default Onboarding